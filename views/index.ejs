<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Random Joke Generator</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/style.css">
</head>
<body>

  <h1>Welcome to random joke generator</h1>

  <form action="/submitForm" method="post">
    <div class="form-row">
      <div class="form-group col-12">
        <label for="exampleInputEmail1">Email address</label>
        <input type="text" class="form-control form-control-lg" name="name" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter a word to make a joke">
        <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
      </div>
    </div>
    <!-- ... rest of your form ... -->
    <button type="submit" class="btn btn-primary btn-lg">Submit</button>
  </form>
  <% if (locals.data && locals.data.error === false) { %>
    <div class="container my-5">
      <div class="p-5 text-center bg-body-tertiary rounded-3">
        <h2 class="text-body-emphasis"><%= locals.data.joke %></h2>
      </div>
    </div>
  <% } else if (locals.data && locals.data.error === true) { %>
    <div class="container my-5">
      <div class="p-5 text-center bg-body-tertiary rounded-3">
        <h2 class="text-body-emphasis"><%= locals.data.message %></h2>
      </div>
    </div>
  <% } %>
</body>
</html>